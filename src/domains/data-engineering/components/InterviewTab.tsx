// DEPRECATED — the topic-scoped InterviewTab has been replaced by the
// tech-scoped InterviewPrepTab (which loads /content/interview/<tech>/*.json).
// This file is intentionally left as an empty module; it is safe to delete.
export {};
